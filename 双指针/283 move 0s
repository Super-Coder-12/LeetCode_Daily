def moveZeroes(nums):
    j = 0  # 指向非零元素应该放置的位置

    for i in range(len(nums)):
        if nums[i] != 0:
            nums[j], nums[i] = nums[i], nums[j]
            j += 1

############ 不要管后半部分 ##########
